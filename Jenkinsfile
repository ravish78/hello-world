pipeline { 
    agent any

    stages {

        stage('Run Script') {
            steps {
                sh 'chmod +x helloworldcode.sh'
                sh './helloworldcode.sh'
            }
        }
    }
}
